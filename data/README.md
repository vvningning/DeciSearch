# Data

This repository includes only small samples under `data/samples/`.

Full benchmark files, cloned repositories, worktrees, teacher trajectories, and SFT outputs are not committed. Generate or download those assets separately, then pass their paths to `run.py` and the scripts in `scripts/`.
